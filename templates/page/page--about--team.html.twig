{% extends '@sacda/page/page.html.twig' %}

{% block content %}

	{% import '@sacda/macros/about-subnav.twig' as subnav %}
	{{ subnav.render('team') }}

	<main role="main" class="pt-10 md:pt-12 pb-16 md:pb-24 lg:pb-28">
		{# Sitewide content container. region--content.html.twig skips its own cap
		   on /about* (self_widthed_content), so it is applied here. #}
		<div class="content max-w-5xl mx-auto px-4 md:px-16">
			{{ page.content }}
		</div>
	</main>
{% endblock %}
