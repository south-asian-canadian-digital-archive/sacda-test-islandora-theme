
{# {% if content %} #}
	<div{{ attributes.addClass('filters', 'result-facets', 'min-h-[10vh]') }}>
		<h4 class="border-y border-border p-4">
			<div class="flex items-center text-lg! h-10">Filter Results</div>
		</h4>
		<div class="filters-content">
			{{ content }}
		</div>
	</div>
{# {% endif %} #}
