
{# {% if content %} #}
	<div class="filters min-h-[10vh]">
		<h2 class="text-xl font-semibold my-4 pb-2 px-4 border-b border-b-[#e5e5e5]">Filters</h2>
		<div class="filters-content">
			{{ content }}
		</div>
	</div>
{# {% endif %} #}
