{#
/**
 * @file
 * Override for highlighted region.
 */
#}
{% if content %}
	<div{{ attributes.addClass('region', 'region-highlighted', 'max-w-5xl', 'mx-auto', 'px-4', 'md:px-16') }}>
		{{ content }}
	</div>
{% endif %}
