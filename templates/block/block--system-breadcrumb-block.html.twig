{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
<div{{ attributes.addClass('breadcrumb_wrapper') }}>
    {{ title_prefix }}
    {{ content }}
    {{ title_suffix }}
</div>