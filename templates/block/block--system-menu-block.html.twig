{#
/**
 * @file
 * Generic custom block template for system menu blocks in SACDA theme.
 * Acts as a pass-through to let the menu template handle all structure/semantics.
 * Need to keep this file idk why but everything breaks without it.
 */
#}
{% block content %}
	{{ content }}
{% endblock %}
