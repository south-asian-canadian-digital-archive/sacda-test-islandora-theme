{# keeping this file so the pager doesn't get overwritten by block--advanced-search.html.twig #}

<div{{ attributes }}>
{{ title_prefix }}
<!--<h2 class="block-title">{{ label }}</h2>-->
{{ title_suffix }}
{{ content }}
</div>