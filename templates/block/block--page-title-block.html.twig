{#
/**
 * @file
 * Theme override for the page title block.
 * Only renders when there's no hero section (hero already shows the title).
 */
#}
{% if content and not sacda_hero_active %}
    {{ title_prefix }}
    <!--<div{{ attributes.addClass('') }}>
        {# {{ content['#title'] }} #}
    </div>-->
    {{content}}
    {{ title_suffix }}
{% endif %}

